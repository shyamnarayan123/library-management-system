spring:
  application:
    name: SpringJpaExample1Application2
    
  datasource:
    url: jdbc:mysql://localhost:3306/shyamdb
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true